# S41 Journal — 2026-05-30

Tags: #infrastructure #dashboard #aura-thai #menu-prices #COGS #architecture

## WHAT WE DID

### 1. Decision Dashboard → ops.radrooster.co (A-07) ✅ DONE
The S40 plan executed cleanly. Apps Script now has a `?mode=json` branch — same code, different output format. Static HTML at `dashboard/aura-thai.html` fetches the JSON endpoint and renders the full dashboard client-side. Fourth tab added to ops: 🍜 Aura Thai.

The architecture held. One-way door rule from S40 ("no standalone dashboards") was respected without having to debate it — the file structure made it the obvious path.

Chris redeployed Apps Script twice this session — once for the initial integration, once at session end after a URL change. The checklist now shows green ✓ for menu prices.

### 2. Menu Prices — Partial Load
Read the Menu Food Price Spreadsheet (Price Chart tab). Got Appetizers, Soups, Salads, Noodles — 48 items total. Added as a collapsible static section to `aura-thai.html`. Dine-in vs DD/UE (+20%) side by side.

Hit a wall at the remaining categories (Fried Rice, Curries, Entrees): Drive API overflowed on the full file, Chrome wasn't running. The section is marked with a yellow warning. S42 opens with Chrome open and this gets finished in the first 5 minutes.

### 3. COGS Tracking — Scoped (A-09)
Chris asked to scope COGS tracking. Here's what got defined:

**The workflow:**
- Chris has purchase invoices — he'll take photos and share via Google Photos in Cowork sessions
- Claude reads the image → extracts vendor, date, line items (ingredient, unit, qty, unit price)
- Output goes into a new `COGS Tracker` tab in `aura_thai_finance`
- Eventually becomes a new ops dashboard tab

**Key clarification:** Tiller does NOT do receipt photos. It's bank-feed only via Plaid — it sees "SJ Distributors, $527.87" but not what was in the order. For itemized cost data, the Google Photos → Claude extraction path is the right call.

The target is 28–32% food cost as % of revenue. We don't know what it is right now. That's A-09.

## KEY DECISIONS

- **Apps Script URL changed again** — Chris redeployed, new URL: `AKfycbwpqQUUUovr5zjSxKvQ5Fx4bO2zzVZ6PG5VM-C3uL7ATcLl1y9RnwE57ElPezVaeVG7/exec` — this is updated in `aura-thai.html` and on GitHub.
- **Menu prices: calculate delivery as ×1.20** — Col E in the spreadsheet had inconsistent values (some valid, some catering bulk prices). Used dine-in × 1.20 per the column header description for clean consistency.
- **COGS via Google Photos** — confirmed as the invoice source. No new app needed.

## FILES CREATED/MODIFIED

| File | Location | Action |
|------|----------|--------|
| `aura-thai.html` | dashboard/ GitHub | Built + updated (URL + menu prices) |
| `index.html` | dashboard/ GitHub | Added Aura Thai tab |
| `decision_dashboard.gs` | outputs/ | Updated (JSON mode + menu prices done:true) |
| `SPRINT.md` | GitHub 00-load-me/ | Updated (A-07 DONE, A-09 details, S42 command) |
| `SESSION_HISTORY.md` | GitHub session-history/ | S40 + S41 added |
| `EMPIRE_STATUS.md` | GitHub empire-status/ | Dashboard live, COGS scoped |
| `RPG_LEDGER.md` | GitHub indexes/ | S40 + S41 XP added |
| `checkpoint_S41_2026-05-30.md` | outputs/ | Written |

## NEXT SESSION — S42

1. **Open Chrome first** — needed to pull remaining menu categories from Price Chart tab
2. **Complete menu** (Fried Rice, Curries, Entrees, Desserts) → push updated `aura-thai.html`
3. **Paste May Lavu data** into Lavu Revenue tab → dashboard KPIs go live
4. **Start A-09** — drop Google Photos invoice images → Claude extracts into COGS Tracker tab

## XP: +70 (S41)
Shipped the integration. COGS has a path. System one step tighter.
